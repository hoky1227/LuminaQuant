# Runtime evidence — profit_moonshot_adaptive_momentum_120_mode

The first tee target was created after the run started, so the full `/usr/bin/time -v` output is preserved in the terminal transcript rather than `${mode}_time.log`.

Observed `/usr/bin/time -v` values:

- Elapsed wall clock: `21:03.54`
- Maximum resident set size: `4,567,432 KB`
- Exit status: `0`
